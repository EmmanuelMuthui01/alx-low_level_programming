#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 if it returns name of the file it was compiled from.
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
